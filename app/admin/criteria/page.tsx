import { saveChecklistItem } from "@/app/actions";
import { Badge, Button, Card, Field, SecondaryButton, inputClass } from "@/components/ui";
import { importanceLabel } from "@/lib/format";
import { prisma } from "@/lib/prisma";

export default async function CriteriaPage() {
  const [items, types] = await Promise.all([prisma.checklistItem.findMany({ include: { facilityType: true }, orderBy: { createdAt: "desc" }, take: 120 }), prisma.facilityType.findMany({ orderBy: { name: "asc" } })]);
  return <div className="space-y-6"><div><h1 className="text-3xl font-black text-official">إدارة المعايير</h1><p className="mt-2 text-muted">إضافة وتعديل وربط المعايير بنوع المنشأة ومستوى الحساسية. يتوفر الاستيراد من Excel عبر ملف مطابق للأوراق المطلوبة.</p></div><Card><form action={saveChecklistItem} className="grid gap-4 md:grid-cols-3"><Field label="رقم البند"><input name="itemNumber" className={inputClass} required/></Field><Field label="القسم الرئيسي"><select name="mainSection" className={inputClass}><option>المواصفات الفنية والمبادئ</option><option>الضوابط التشغيلية</option><option>الملاحق والتصنيفات</option></select></Field><Field label="التصنيف الفرعي"><input name="subCategory" className={inputClass} required/></Field><Field label="نوع المنشأة"><select name="facilityTypeId" className={inputClass}>{types.map((t) => <option key={t.id} value={t.id}>{t.name}</option>)}</select></Field><Field label="مستوى الحساسية"><select name="sensitivityLevel" className={inputClass}><option>عالية</option><option>متوسطة</option><option>منخفضة</option></select></Field><Field label="درجة الأهمية"><select name="importance" className={inputClass}><option value="HIGH">عالية</option><option value="MEDIUM">متوسطة</option><option value="LOW">منخفضة</option></select></Field><Field label="حالة الإلزام"><select name="requirementStatus" className={inputClass}><option value="MANDATORY">إلزامي</option><option value="CONDITIONAL">مشروط</option></select></Field><Field label="المرجع النظامي"><input name="regulatoryReference" className={inputClass}/></Field><Field label="رقم المادة أو الفقرة"><input name="articleNumber" className={inputClass}/></Field><Field label="نص المتطلب"><textarea name="requirementText" className={inputClass + " md:col-span-3"} required/></Field><Button className="md:col-span-3">إضافة معيار</Button></form></Card><Card><div className="mb-4 flex gap-3"><SecondaryButton href="/api/criteria/export?format=json">تصدير JSON</SecondaryButton><SecondaryButton href="/api/criteria/export?format=xlsx">تصدير Excel</SecondaryButton></div><div className="overflow-x-auto"><table className="w-full text-sm"><thead><tr className="border-b text-muted"><th className="p-3 text-right">البند</th><th className="p-3 text-right">القسم</th><th className="p-3 text-right">النص</th><th className="p-3 text-right">نوع المنشأة</th><th className="p-3 text-right">الحساسية</th><th className="p-3 text-right">الأهمية</th><th className="p-3 text-right">الحالة</th></tr></thead><tbody>{items.map((i) => <tr key={i.id} className="border-b last:border-0"><td className="p-3 font-bold">{i.itemNumber}</td><td className="p-3">{i.mainSection}<br/><span className="text-xs text-muted">{i.subCategory}</span></td><td className="p-3 max-w-xl">{i.requirementText}</td><td className="p-3">{i.facilityType?.name ?? "عام"}</td><td className="p-3">{i.sensitivityLevel}</td><td className="p-3"><Badge tone={i.importance === "HIGH" ? "danger" : i.importance === "MEDIUM" ? "warning" : "muted"}>{importanceLabel(i.importance)}</Badge></td><td className="p-3">{i.isActive ? "مفعل" : "معطل"}</td></tr>)}</tbody></table></div></Card></div>;
}
